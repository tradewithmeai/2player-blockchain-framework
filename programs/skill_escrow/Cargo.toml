[package]
name = "skill_escrow"
version = "0.1.0"
description = "Match escrow and settlement for skill-based games"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "skill_escrow"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
anchor-spl = "0.30.1"
solana-program = "1.18.22"
